class Note {
    noteName: string;
    noteNum: number;
    constructor(noteName: string, noteNum: number) {
        this.noteName = noteName;
        this.noteNum = noteNum;
    }
}

export { Note };
